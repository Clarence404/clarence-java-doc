# Java总结-数据库

## 一、数据库的隔离级别？

uncommited read

commited read

repeatable read

serializable read

::: tip
MySQL 默认的隔离级别：Repeatable Read（可重读）级别。<br>
Oracle 默认的隔离级别：Read Committed（读取已提交的）级别。
:::

## 二、索引失效的场景有哪些？

## 三、Mysql的存储引擎对比